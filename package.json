{
  "name": "api",
  "version": "1.0.0",
  "description": "Back-end directory.",
  "main": "index.js",
  "engines": {
    "node": "15.x",
    "npm": "7.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js && cd interface && npm install && npm run build",
    "heroku-postbuild": "cd interface && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.0.0",
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "lodash": "^4.17.21",
    "mongoose": "^5.12.6",
    "mongoose-paginate-v2": "^1.3.14"
  }
}
